apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sdwc-ingress-api
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
    traefik.ingress.kubernetes.io/router.priority: "100"
spec:
  rules:
    - http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: sdwc-api
                port:
                  number: 8000
          - path: /health
            pathType: Exact
            backend:
              service:
                name: sdwc-api
                port:
                  number: 8000
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sdwc-ingress-web
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
    traefik.ingress.kubernetes.io/router.priority: "1"
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sdwc-web
                port:
                  number: 80
